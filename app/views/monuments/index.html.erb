<div class="banner-index" style="background-image: url('https://images.pexels.com/photos/1805895/pexels-photo-1805895.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=750&w=1260');">
  <div style="margin: -50px; margin-left: 60px;">
    <h1>WHAT WE FOUND FOR YOU</h1>
    <p>Find inspiration for your events.</p>
  </div>
</div>

<div class="container-title">
  <h2>OUR FANTASTIC SPACES ✨</h2>
</div>
<hr>
<div class="container">
  <div class="row">
    <div class="col-8">
      <div class="row">
        <% @monuments.each do |monument| %>
        <div class="col-6" style="padding: 10px;">
          <div class="card-trip">
              <% if monument.photo.attached? %>
                <%= cl_image_tag monument.photo.key, height: 300, width: 400, crop: :fill %>
              <% end %>
              <div class="card-trip-infos">
                <div>
                  <h2><%= link_to monument.name, monument_path(monument) %></h2>
                  <p><%= monument.city %></p>
                </div>
                <h2 class="card-trip-pricing">$<%= monument.price  %>M</h2>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
      <div class="col-4" id="map"
     style="width: 90%;
     height: 500px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
    </div>
</div>
